# QuestDB Log Configuration
# Only show ERROR level logs to stdout (suppresses INFO and ADVISORY)
# QuestDB levels: DEBUG < INFO < ADVISORY < ERROR < CRITICAL

writers=stdout

# stdout writer - ERROR and above only
w.stdout.class=io.questdb.log.LogConsoleWriter
w.stdout.level=ERROR
