# =============================================================================
# WAN Monitor Environment Configuration
# =============================================================================
# Copy this file to .env.local (for development) or .env (for Docker)
#
# For development:
#   cp .env.example .env.local
#   pnpm dev
#
# For Docker:
#   cp .env.example .env
#   docker-compose -f docker-compose.prod.yml up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
SERVER_PORT=3001
SERVER_HOST=0.0.0.0

# -----------------------------------------------------------------------------
# Database Configuration (QuestDB)
# -----------------------------------------------------------------------------
# For development: point to local or Docker QuestDB instance
# For production Docker: uses embedded QuestDB (localhost)
DB_HOST=localhost
DB_PORT=9000
DB_PROTOCOL=http
DB_AUTO_FLUSH_ROWS=100
DB_AUTO_FLUSH_INTERVAL=1000
DB_REQUEST_TIMEOUT=10000
DB_RETRY_TIMEOUT=1000

# -----------------------------------------------------------------------------
# Ping Configuration
# -----------------------------------------------------------------------------
PING_TIMEOUT=5
PING_TRAIN_COUNT=10
PING_HOSTS=8.8.8.8,1.1.1.1,cloudflare.com

# -----------------------------------------------------------------------------
# Authentication Configuration
# -----------------------------------------------------------------------------
# Username for dashboard login (default: admin)
WAN_MONITOR_USERNAME=admin

# Password for dashboard login
# REQUIRED to enable authentication - leave empty to disable auth
WAN_MONITOR_PASSWORD=

# Secret key for JWT token signing
# REQUIRED for secure authentication - use a random string (32+ chars recommended)
# Generate with: openssl rand -base64 32
JWT_SECRET=

# JWT token expiration time (default: 24h)
# Examples: 1h, 12h, 24h, 7d
JWT_EXPIRES_IN=24h
